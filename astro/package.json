{
  "name": "hunter-alpha-hub-astro",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-og-images.mjs && node scripts/finalize-redirects.mjs",
    "check:theme": "node scripts/check-theme-tokens.mjs",
    "check:content": "node scripts/check-content.mjs",
    "check:analytics": "node scripts/check-analytics.mjs",
    "check:markup": "node scripts/check-markup.mjs",
    "check:parity": "node scripts/check-page-parity.mjs",
    "check:files": "node scripts/check-site-files.mjs",
    "checks": "npm run build && npm run check:theme && npm run check:content && npm run check:analytics && npm run check:markup && npm run check:parity && npm run check:files && npm run check:subscription && npm run verify:pages",
    "verify:pages": "node scripts/verify-pages.mjs",
    "import:blog": "node --experimental-strip-types scripts/import-blog.mjs",
    "probe:jev": "node scripts/jev-probe.mjs",
    "preview": "wrangler dev",
    "deploy": "npm run build && node scripts/mark-preview.mjs && wrangler deploy --config wrangler.jsonc",
    "deploy:production": "PUBLIC_ADSENSE_ID=ca-pub-4516753695428109 npm run build && wrangler deploy --config wrangler.production.jsonc",
    "og": "node scripts/generate-og-images.mjs",
    "icons": "node scripts/generate-favicons.mjs",
    "deploy:apex": "wrangler deploy --config wrangler.apex.jsonc",
    "check:subscription": "node scripts/check-subscription.mjs",
    "db:migrate": "CLOUDFLARE_ACCOUNT_ID=de677f38b96340f3a63a53e5044a1c98 wrangler d1 migrations apply hunter-alpha-hub-subscribers --remote",
    "db:migrate:local": "wrangler d1 migrations apply hunter-alpha-hub-subscribers --local"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.127.1",
    "sharp": "^0.35.4"
  },
  "engines": {
    "node": ">=22"
  }
}
